
de:
  flapdoodle:
    mongodb:
      embedded:
        version: 7.0.2

spring:
  data:
    mongodb:
      host: localhost
      port: 27017
      database: gestion_colis_test

jwt:

  secret: test-secret-key-for-unit-tests-only-must-be-long-enough
  expiration: 3600000

logging:
  level:
    root: ERROR
    com.logistics.parcelandcarrier: DEBUG
    de.flapdoodle.embed.mongo: WARN
